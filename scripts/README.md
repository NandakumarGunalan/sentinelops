# Scripts

Utility scripts for development and ops tasks.

Planned:
- `seed_db.py`     — populate SQLite with sample events
- `reset_db.py`    — drop and recreate the database
- `run_agent.py`   — trigger the LangGraph pipeline manually
